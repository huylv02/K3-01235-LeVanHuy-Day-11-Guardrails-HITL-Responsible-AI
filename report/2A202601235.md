# Báo cáo Bài lab 11 - Lê Văn Huy - MSSV: 2A202601235

## 1. Phần A: Triển khai Phòng thủ (Guardrails)
Trong phần này, tôi đã thực hiện các nhiệm vụ sau để bảo vệ Agent:

- **Input Guardrails (TODO 1-3):**
  - Đã triển khai `detect_injection()` để phát hiện các kỹ thuật tấn công injection phổ biến.
  - Đã cấu hình `topic_filter()` để đảm bảo Agent chỉ trả lời các chủ đề về ngân hàng.
  - Đã tích hợp `InputGuardrailPlugin` để chặn các yêu cầu độc hại ngay từ đầu.
- **Output Guardrails (TODO 4-6):**
  - Đã triển khai `content_filter()` để tự động thay thế PII (SĐT, email, API key, password) bằng `[REDACTED]`.
  - Đã thiết lập LLM-as-Judge để đánh giá tính an toàn của câu trả lời trước khi gửi tới người dùng.
- **Egress Policy (TODO 8A):**
  - Đã cấu hình `is_egress_allowed` để chỉ cho phép Agent gửi dữ liệu tới domain hợp lệ (`https://api.vinbank.example/`) và loại bỏ dữ liệu nhạy cảm khỏi payload.
- **Kết quả kiểm thử:** Toàn bộ các test case public cho Phần A đều vượt qua (PASS).

## 2. Phần B: Tấn công (Adversarial Attacks - Tóm tắt)
Phần này tập trung vào việc đánh giá khả năng phòng thủ của hệ thống:

- **TODO 13:** Đã viết 5 prompt tấn công nâng cao bao gồm: Encoding (Base64), Context manipulation (Compliance Audit), Roleplay (Admin Simulator), Side-channel, và Multi-step escalation.
- **TODO 14:** Đã triển khai hàm `generate_ai_attacks()` để tự động tạo test case tấn công.
- **Kết quả thực thi:** Mặc dù gặp hạn chế về quota API, các cuộc tấn công thử nghiệm cho thấy hệ thống đã chặn thành công hầu hết các nỗ lực khai thác PII và injection thông qua các lớp guardrail đã triển khai ở Phần A.

## 3. Tổng kết
Hệ thống hiện tại đã được trang bị kiến trúc Defense-in-depth, đảm bảo an toàn cho các hoạt động của Agent VinBank.
